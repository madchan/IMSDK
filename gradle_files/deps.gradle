ext {
    versions = [
            // 依赖库
            lib_base   : "1.0.0-alpha01",
            lib_network: "1.0.0-alpha01",

            // Kotlin相关
            kotlin     : "1.4.21",
            core_ktx   : "1.3.2",

            // 单元测试
            junit      : "4.+",
            junit_ext  : "1.1.2",
            espresso   : "3.3.0",
    ]

    libs = [
            // 依赖库
            lib_base   : "com.litalk.supportlib:lib_base:${versions.lib_base}",
            lib_network: "com.litalk.supportlib:lib_network:${versions.lib_network}",

            // Kotlin相关
            kotlin     : "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}",
            core_ktx   : "androidx.core:core-ktx:${versions.core_ktx}",

            // 单元测试
            junit      : "junit:junit:${versions.junit}",
            junit_ext  : "androidx.test.ext:junit:${versions.junit_ext}",
            espresso   : "androidx.test.espresso:espresso-core:${versions.espresso}"
    ]
}