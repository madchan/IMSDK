apply from:'../gradle_files/base.gradle'

dependencies {
    def lib_base_version = "1.0.0-alpha15"
    def lib_network_version = "1.0.0-alpha01"

    api "com.litalk.supportlib:lib_base:${lib_base_version}"
    api "com.litalk.supportlib:lib_network:${lib_network_version}"
}